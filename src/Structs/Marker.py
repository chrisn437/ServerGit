class Marker():
    """Data structure for defining properties of a Marker
    """
    def __init__(self, ID, LABEL, POS, ROT) -> None:
        self.id = ID
        self.label = LABEL
        self.pos = POS
        self.rot = ROT